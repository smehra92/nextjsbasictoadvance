import React from 'react'

const Loading = () => {
  return (
    <div>Loading...</div>
  )
}

export default Loading





//Since loading is happening too fast, to see it inspect -- >> -- Components -- (Search for)Suspense -- Tap on Clock) Suspended : true.

//We could've also used DaisyUI loading animation here in stead of just " Loading... ".